<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="OllamaHub.Views.PromptLibraryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="using:OllamaHub.Models"
      Background="#0F0F13">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="280"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- LEFT -->
    <Border Grid.Column="0" Background="#13131A"
            BorderBrush="#1E1E2E" BorderThickness="0,0,1,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="14,16,14,10" Spacing="8">
          <StackPanel Orientation="Horizontal" Spacing="10">
            <Border Background="#22FFFFFF" CornerRadius="8" Padding="7">
              <FontIcon Glyph="&#xE8F4;" FontSize="16" Foreground="#8A63FF"/>
            </Border>
            <TextBlock Text="Prompt Library" FontSize="16" FontWeight="SemiBold"
                       Foreground="#EEEEFF" VerticalAlignment="Center"/>
          </StackPanel>
          <Border Background="#1A1A24" BorderBrush="#2A2A3A" BorderThickness="1" CornerRadius="10">
            <TextBox PlaceholderText="Search prompts..."
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     BorderThickness="0" Background="Transparent"
                     Foreground="#DDDDEF" Padding="12,9"/>
          </Border>
        </StackPanel>

        <Button Grid.Row="1" Click="AddNew_Click"
                HorizontalAlignment="Stretch" Margin="14,0,14,10"
                CornerRadius="10" Height="36" BorderThickness="0">
          <Button.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#8A63FF" Offset="0"/>
              <GradientStop Color="#4F9EFF" Offset="1"/>
            </LinearGradientBrush>
          </Button.Background>
          <StackPanel Orientation="Horizontal" Spacing="8">
            <FontIcon Glyph="&#xE710;" FontSize="13" Foreground="White"/>
            <TextBlock Text="New Prompt" Foreground="White" FontWeight="SemiBold" FontSize="13"/>
          </StackPanel>
        </Button>

        <ListView Grid.Row="2" x:Name="TemplateList"
                  ItemsSource="{x:Bind ViewModel.Filtered, Mode=OneWay}"
                  SelectionChanged="TemplateList_SelectionChanged"
                  Background="Transparent" Margin="6,0,6,8">
          <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:PromptTemplate">
              <Border Padding="10,8" CornerRadius="8">
                <Grid ColumnSpacing="10">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                  </Grid.ColumnDefinitions>
                  <Border Grid.Column="0" Background="#22FFFFFF" CornerRadius="8"
                          Width="32" Height="32">
                    <FontIcon Glyph="{x:Bind Icon}" FontSize="15" Foreground="#8A63FF"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                  </Border>
                  <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind Name}" FontSize="13" FontWeight="SemiBold"
                               Foreground="#DDDDEF" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{x:Bind Category}" FontSize="11" Foreground="#666688"/>
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ListView.ItemTemplate>
          <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
              <Setter Property="CornerRadius" Value="8"/>
              <Setter Property="Margin" Value="0,1"/>
              <Setter Property="Padding" Value="0"/>
            </Style>
          </ListView.ItemContainerStyle>
        </ListView>
      </Grid>
    </Border>

    <!-- RIGHT: editor -->
    <Grid Grid.Column="1" Background="#0F0F13" Padding="28">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Grid.Row="0" Text="Edit Prompt" FontSize="22" FontWeight="Bold"
                 Foreground="#EEEEFF" Margin="0,0,0,20"/>

      <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" Margin="0,0,0,14">
        <StackPanel Spacing="6" Width="220">
          <TextBlock Text="NAME" FontSize="10" FontWeight="SemiBold"
                     CharacterSpacing="120" Foreground="#666688"/>
          <Border Background="#1A1A24" BorderBrush="#2A2A3A" BorderThickness="1" CornerRadius="10">
            <TextBox Text="{x:Bind ViewModel.NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="Prompt name..." BorderThickness="0"
                     Background="Transparent" Foreground="#DDDDEF" Padding="12,10"/>
          </Border>
        </StackPanel>
        <StackPanel Spacing="6" Width="160">
          <TextBlock Text="CATEGORY" FontSize="10" FontWeight="SemiBold"
                     CharacterSpacing="120" Foreground="#666688"/>
          <ComboBox x:Name="CategoryBox"
                    ItemsSource="{x:Bind ViewModel.Categories}"
                    SelectedItem="{x:Bind ViewModel.NewCategory, Mode=TwoWay}"
                    HorizontalAlignment="Stretch"
                    Background="#1A1A24" BorderBrush="#2A2A3A"/>
        </StackPanel>
      </StackPanel>

      <TextBlock Grid.Row="2" Text="CONTENT" FontSize="10" FontWeight="SemiBold"
                 CharacterSpacing="120" Foreground="#666688" Margin="0,0,0,6"/>

      <Border Grid.Row="3" Background="#1A1A24" BorderBrush="#2A2A3A"
              BorderThickness="1" CornerRadius="12">
        <TextBox Text="{x:Bind ViewModel.NewContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 PlaceholderText="Write your prompt template here..."
                 AcceptsReturn="True" TextWrapping="Wrap"
                 BorderThickness="0" Background="Transparent"
                 Foreground="#DDDDEF" Padding="16,14"
                 FontFamily="Cascadia Code, Consolas, monospace" FontSize="13"
                 VerticalAlignment="Stretch"/>
      </Border>

      <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
        <Button Click="Save_Click" CornerRadius="10" Height="38" Padding="16,0" BorderThickness="0">
          <Button.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
              <GradientStop Color="#8A63FF" Offset="0"/>
              <GradientStop Color="#4F9EFF" Offset="1"/>
            </LinearGradientBrush>
          </Button.Background>
          <StackPanel Orientation="Horizontal" Spacing="8">
            <FontIcon Glyph="&#xE74E;" FontSize="13" Foreground="White"/>
            <TextBlock Text="Save Prompt" Foreground="White" FontWeight="SemiBold"/>
          </StackPanel>
        </Button>
        <Button Click="UsePrompt_Click" CornerRadius="10" Height="38" Padding="16,0"
                Background="#1A1A24" BorderBrush="#2A2A3A" Foreground="#DDDDEF">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <FontIcon Glyph="&#xE8BD;" FontSize="13" Foreground="#4F9EFF"/>
            <TextBlock Text="Use in Chat"/>
          </StackPanel>
        </Button>
        <Button x:Name="DeleteBtn" Click="Delete_Click" CornerRadius="10"
                Height="38" Padding="16,0" Background="#1E1A1A"
                BorderBrush="#3A2A2A" Foreground="#FF6B6B">
          <StackPanel Orientation="Horizontal" Spacing="8">
            <FontIcon Glyph="&#xE74D;" FontSize="13" Foreground="#FF6B6B"/>
            <TextBlock Text="Delete"/>
          </StackPanel>
        </Button>
      </StackPanel>
    </Grid>
  </Grid>
</Page>
